<%= form_for(@the_post) do |f| %>
  <% if @the_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@the_post.errors.count, "error") %> prohibited this the_post from being saved:</h2>

      <ul>
      <% @the_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
